{
  "name": "beignet-haricot",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "sync:config": "node scripts/sync-app-config.mjs",
    "predev": "npm run sync:config",
    "dev": "vite",
    "prebuild": "npm run sync:config",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "release:check": "node scripts/release-info.mjs check",
    "pretauri": "npm run sync:config",
    "tauri": "tauri",
    "check": "npm run build && cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "lucide-preact": "^0.468.0",
    "preact": "^10.25.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "@tauri-apps/cli": "^2.0.0",
    "@types/node": "^20.19.43",
    "typescript": "~5.7.0",
    "vite": "^6.0.0"
  }
}
